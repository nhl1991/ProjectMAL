
export default function ResultsFooter({ children }: {children:React.ReactNode}){

    return(
        
      <footer className="py-4 flex items-center justify-center">
        {children}
      </footer>
    )
}